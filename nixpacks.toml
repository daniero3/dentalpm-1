[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = [
  "cd dental-pm-mvp && npm ci",
  "cd dental-pm-mvp/frontend && npm install"
]

[phases.build]
cmds = [
  "cd dental-pm-mvp/frontend && npm run build"
]

[start]
cmd = "cd dental-pm-mvp && node server.js"
