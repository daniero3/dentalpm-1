[phases.setup]
nixPkgs = ["nodejs"]

[phases.install]
cmds = [
  "npm ci",
  "cd ../frontend && npm install"
]

[phases.build]
cmds = [
  "cd ../frontend && npm run build"
]

[start]
cmd = "node server.js"
